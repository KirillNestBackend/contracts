{
  "name": "@kirill-nest-backend/contracts",
  "version": "1.0.3",
  "description": "Protobufs definitions for the project",
  "scripts": {
    "generate": "protoc -I ./proto ./proto/*.proto --ts_proto_out=./gen --ts_proto_opt=nestJs=true,package=omit"
  },
  "files": [
    "gen",
    "proto"
  ],
  "license": "ISC",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@nestjs/microservices": "^11.1.12",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "ts-proto": "^2.7.5"
  }
}
